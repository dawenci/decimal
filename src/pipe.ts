import { Num } from "./num"

type Fn = (n: any) => string

export function pipe(...fns: Fn[]) {

}

// export const add: Fn = (n: any) => {
//   const _n = new _Num(n)
//   return (n2: any) => {
//     // return new _n + _Num(n2)
//   }
// }
